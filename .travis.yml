sudo: required 
language: node_js
node_js:
    - "stable"
    
services: 
    - docker
before_install: 
    - docker build -t kevinli92924/sample-docker-test -f Dockerfile.dev .
script:     
    - docker run -e CI=true kevinli92924/sample-docker-test npm run test -- --coverage --watchAll=false