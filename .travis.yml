sudo: required 
language: node_js
node_js:
    - "stable"
    
services: 
    - docker
before_install: 
    - docker build -t kevinli92924/sample-app -f Dockerfile.dev .
script:     
    - docker run -e CI=true kevinli92924/sample-app npm run test -- --coverage --watchAll=false